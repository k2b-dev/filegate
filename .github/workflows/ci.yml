name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  vet-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: go build
        run: go build ./...
      - name: go vet
        run: go vet ./...
      - name: go test
        run: go test -count=1 ./...

  race:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: full race suite
        run: make test-race

  vulnerability-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - name: scan reachable Go code
        run: |
          "$(go env GOPATH)/bin/govulncheck" ./...

  staticcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@latest
      - name: run staticcheck
        run: |
          out=$("$(go env GOPATH)/bin/staticcheck" ./... 2>&1)
          if echo "$out" | grep -q "matched no packages"; then
            echo "staticcheck saw no packages"
            exit 1
          fi
          if [ -n "$out" ]; then
            echo "$out"
            exit 1
          fi
          echo "staticcheck: clean"

  detector-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: detector + index sync regression tests
        run: make test-detector-linux

  detector-btrfs:
    # Real btrfs detector test on a loopback btrfs image inside a privileged
    # container. GitHub-hosted ubuntu-latest supports privileged Docker, so
    # we can exercise the real `btrfs find-new` path on every push without
    # needing a self-hosted runner.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: real-btrfs detector test (loopback image)
        run: make test-detector-btrfs-real-docker
      - name: real-btrfs versioning test (loopback image)
        run: make test-versioning-btrfs-real-docker

  docker-runtime-state:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: runtime state survives container recreation
        run: make test-docker-runtime-state

  fuzz-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: fuzz smoke (10s per target)
        run: make fuzz-smoke

  ts-sdk-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"
      - name: install
        working-directory: sdk/ts
        run: npm ci
      - name: audit production dependencies
        working-directory: sdk/ts
        run: npm audit --omit=dev
      - name: build
        working-directory: sdk/ts
        run: npm run build
      - name: test
        working-directory: sdk/ts
        run: bun test test

  admin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"
      - name: install
        working-directory: admin
        run: bun install --frozen-lockfile
      - name: typecheck
        working-directory: admin
        run: bun run typecheck
      - name: test
        working-directory: admin
        run: bun test --isolate
      - name: build
        working-directory: admin
        run: bun run build
      - name: audit production dependencies
        working-directory: admin
        run: bun audit --production
      - name: build container
        run: docker build --file admin/Dockerfile .
