# Point Filegate server.public_url at this HTTPS origin.
# Access logging is deliberately omitted: signed URLs are bearer capabilities.
{$FILEGATE_HOST} {
    reverse_proxy filegate:8080 {
        flush_interval -1
    }
}
